Create delete/s                                     Create a new user directory

        This command creates a directory for the logged on user, which allows
people to send mail.

        If delete is quoted, the user's directory is deleted.


